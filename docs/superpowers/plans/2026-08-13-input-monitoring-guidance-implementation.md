# 输入监控权限引导实施计划

日期：2026-08-13

1. 在核心模块加入可测试的权限缺失状态与输入监控引导决策，覆盖首次自动触发、轮询不重复、手动可重复及权限就绪分支。
2. 拆分 `PermissionController` 的辅助功能请求和输入监控请求，并使打开系统设置返回是否成功。
3. 新增 AppKit 权限说明弹窗；首次缺少输入监控时显示一次，菜单手动检查时可再次显示。
4. 让运行状态精确显示缺少的权限，确保定时轮询只更新状态、不重复弹窗。
5. 更新 README 和版本号，运行自动测试与严格编译，生成不会覆盖现有 Finder 替身的版本化应用和压缩包。
