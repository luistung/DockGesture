# DockGesture 应用图标实施计划

日期：2026-08-13

1. 创建 A1 蓝紫方向的可维护 SVG 源图，使用精确坐标绘制底板、Dock、应用方块、箭头和黄色落点。
2. 创建适合 16px/32px 的简化小尺寸 SVG，增粗关键描边并减少透明层次。
3. 用本机矢量渲染工具输出标准 `.iconset` 所需 PNG，检查尺寸、透明通道与小尺寸可辨识度。
4. 使用 `iconutil` 生成 `DockGesture.icns`，并验证可反向解包。
5. 在 Info.plist 和构建脚本中接入图标，更新版本至 1.0.2，生成新应用和压缩包。
6. 运行 33 项核心检查、资源检查、签名验证和压缩包完整性检查，提交实现。
